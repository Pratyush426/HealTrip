{
    "name": "healtrip-backend",
    "version": "1.0.0",
    "description": "HealTrip Medical Tourism Platform - Main Backend Service",
    "main": "src/server.js",
    "type": "module",
    "scripts": {
        "start": "node src/server.js",
        "dev": "nodemon src/server.js",
        "dev:safe": "node -e \"const {execSync}=require('child_process');try{execSync('Stop-Process -Id (Get-NetTCPConnection -LocalPort 5000 -ErrorAction SilentlyContinue).OwningProcess -Force -ErrorAction SilentlyContinue',{shell:'powershell'})}catch(e){}\" && nodemon src/server.js",
        "kill": "powershell -Command \"Stop-Process -Id (Get-NetTCPConnection -LocalPort 5000 -ErrorAction SilentlyContinue).OwningProcess -Force -ErrorAction SilentlyContinue; Write-Host 'Port 5000 cleared'\"",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "medical-tourism",
        "healthcare",
        "booking",
        "api"
    ],
    "author": "HealTrip Team",
    "license": "ISC",
    "dependencies": {
        "@clerk/clerk-sdk-node": "^4.13.0",
        "cloudinary": "^1.41.0",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "mongoose": "^8.0.3",
        "multer": "^1.4.5-lts.1",
        "openai": "^6.10.0",
        "razorpay": "^2.9.2",
        "stripe": "^14.8.0"
    },
    "devDependencies": {
        "nodemon": "^3.0.2"
    }
}
